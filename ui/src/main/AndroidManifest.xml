<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ys.ui">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        <activity
            android:name=".activity.InitActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity
            android:name=".activity.GetProductActivity"
            android:label="@string/title_activity_get_product"
            
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.PrintActivity"
            android:label="@string/print"
            
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.TermInitActivity"
            android:label="@string/term_init"
            
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.AdminLoginActivity"
            android:label="@string/login"
            
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.AdminActivity"
            android:label="@string/admin"
            
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.QRcodeActivity"
            android:label="二维码支付"
            
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ProductActivity"
            android:label="商品购买"
            
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ProductDetailActivity"
            android:label="商品详情"
            
            android:theme="@style/AppTheme.NoActionBar" />

         <activity android:name=".activity.OutGoodsActivity"
            android:label="出货"
            
            android:theme="@style/AppTheme.NoActionBar" />
         <activity android:name=".activity.PayActivity"
            android:label="预支付"
            
            android:theme="@style/AppTheme.NoActionBar" />
     <activity android:name=".activity.QRcodePayActivity"
                android:label="支付"
                
                android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".activity.HomeActivity"
            android:label="首页"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />


        <!-- <activity android:name="com.ys.ui.activity.MainActivity"></activity> -->
        <activity android:name=".sample.SelectGoodsActivity" />
        <activity android:name=".sample.MainMenu" />

        <activity android:name=".serial.salemachine.SerialPortPreferences"></activity>

        <!--打印机测试-->
        <!-- <activity android:name="com.ys.test.activity.SerialPortActivity"/>-->
        <activity android:name=".serial.print.activity.MainMenu"
            
            android:theme="@style/AppTheme.NoActionBar"/>

        <service android:name=".service.MyService" />
        <service android:name=".service.TimerService" />

        <receiver android:name=".service.AlarmReceiver" />


    </application>

</manifest>